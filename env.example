# Database Configuration
DATABASE_URL="postgresql://username:password@localhost:5432/devportfolio"

# Node Environment
NODE_ENV="development"
 
# Optional: For development without database (uses in-memory storage)
USE_MEMORY_STORAGE="true" 